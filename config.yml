is_debug: true
listen:
  type: port
  bind_ip: 0.0.0.0
  port: 10005
storage:
  type: sql
  mongodb:
    host: localhost
    port: 27017
    database: rest2
    authdb:
    user:
    password:
    collection: users
  postgresql:
    host: 0.0.0.0
    port: 5432
    database: postgres
    user: postgres
    password: postgres